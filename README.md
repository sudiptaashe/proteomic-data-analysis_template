# proteomic-data-analysis_template

This template covers proteomic data set analysis. Before starting the analysis, the datsets has to be cleaned to remove unwanted MS detections. The filtered list is then used to running this template- filtering, PCA analysis, log2fc calculation, volcano plots, heatmaps and ranked gene pathway analysis. The py version can be different, so look for additional package installations if required.
